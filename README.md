Back-end Laravel
Front-end React

A simple project that does API CRUD operations and has an interface

Instructions:

Laravel:

1. Clone the project
2. Run composer install
3. Run php artisan

React:

1. Run npm install
2. Run npm start
